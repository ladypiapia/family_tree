# Family Tree 项目沟通与介绍

最后更新：2026-02-26

## 1. 项目介绍

这是一个面向走亲访友场景的家族族谱 Web 应用，目标是帮助用户：

- 可视化查看家族成员关系（支持多家族）
- 快速查询两人间的亲戚称呼（中国大陆常见称谓）
- 支持自定义称呼，适配地域差异
- 本地存储隐私数据（含头像）并支持 JSON 导入导出

技术栈：`React + TypeScript + Vite + Tailwind CSS + React Flow + idb`

## 2. 沟通决策记录（摘要）

### 2.1 初始范围确认

- 先做 V1 单用户版
- 不做注册登录
- 称呼规则按中国大陆常见称谓

### 2.2 重置开发策略

- 按 `PRD.md` 全量重建项目
- 删除旧的原型代码（HTML 单页版本）
- 从 0 搭建 Vite + React + TS 工程

### 2.3 已完成的关键修复与迭代

1. 配偶布局优化
- 由“配偶与子女分堆”改为“配偶优先相邻展示”。

2. 成员新增时支持自定义称呼
- 新增成员表单支持录入“自定义称呼 + 称呼视角（谁称呼 TA）”。

3. 视觉规则调整
- 已故成员改为灰色填充
- 男性默认头像为蓝色风格
- 女性默认头像为粉色风格

4. 页面结构改造为 Tab
- 采用顶部 Tab 菜单分区：`首页图谱 / 家族管理 / 成员管理 / 关系管理 / 称呼查询`
- 首页仅展示关系图谱

5. 关系图谱进一步优化
- 同代节点按“家庭单元”聚合（父母家庭/配偶家庭）后再排序，减少随机分散
- 横向与组间间距进一步拉大，改善可读性

## 3. 当前功能清单

- 多家族管理：创建、切换、删除
- 成员管理：新增/编辑/删除、出生日期、在世状态、备注、头像上传
- 关系管理：父母、配偶/伴侣、兄弟姐妹
- 图谱展示：React Flow 可拖拽、缩放、小地图
- 中心视角：点击节点切换中心成员
- 称呼查询：任意两人关系推断 + 路径展示
- 称呼覆盖：手动保存方向型称呼（A -> B）
- 数据存储：idb（并带 localStorage 回退）
- 数据迁移：JSON 导入导出

## 4. 主要文件

- `src/App.tsx`：主界面、Tab、图谱布局、交互逻辑
- `src/lib/kinship.ts`：关系路径与称呼推断
- `src/lib/storage.ts`：本地持久化（idb + fallback）
- `src/types.ts`：核心数据类型
- `README.md`：运行与功能说明
- `PRD.md`：需求原文

## 5. 运行方式

```bash
npm install
npm run dev
```

构建验证：

```bash
npm run build
```

## 6. 后续可继续项（建议）

- 称呼规则精细化（伯/叔、堂/表、姻亲更细颗粒）
- 图谱自动布局参数做“紧凑/舒展”模式切换
- 增加关系冲突检测（循环祖先、角色冲突等）
- 增加移动端底部导航（更接近 App 交互）
